spring:
  application:
    name: promo-api
  
  datasource:
    url: jdbc:mysql://localhost:3306/promodb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: walidA31+
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

jwt:
  secret: mySecretKeyForJWTTokenGenerationThatIsLongEnough
  expiration: 86400000
